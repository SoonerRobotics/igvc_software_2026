namespace {NAMESPACE_PLACEHOLDER};

table CanFrame {
    // The timestamp of the can frame in microseconds
    timestamp: ulong;

    // The sequence number of the can frame
    sequence_number: uint;

    // The Can Id
    can_id: uint;

    // The can data
    can_data: [ubyte];
}

root_type CanFrame;