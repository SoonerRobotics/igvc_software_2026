using igvc_sharp;

// using var src = new Mat(FileUtilities.GetFileRelativeToRoot("pattern.png", "resources"), ImreadModes.Grayscale);
// using (new Window("src image", src)) 
// {
//     Cv2.WaitKey();
// }

await RuntimeManager.Begin();
Logging.Dispose();